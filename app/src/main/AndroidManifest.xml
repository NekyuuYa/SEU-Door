<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <!-- 显式绑定支付宝 IAlixPay service 需要包可见性（targetSdk 30+） -->
    <queries>
        <package android:name="com.eg.android.AlipayGphone" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DoorOpener">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                NFC 唤起：app 未在前台时贴门锁标签自动拉起。
                门锁标签 NDEF 第一条是 URI(https://uc-zhuli.whxinna.com/?t=lock&d=...)，
                第二条是 AAR(com.whxinna.userplatform 官方 app)。系统派发顺序：
                  1) 先尝试 AAR 指定包；该包未安装时
                  2) 回落到普通 NDEF 派发 → 命中下面这个精确 host 过滤器 → 唤起本 app。
                只精确匹配该 host，避免被任意网址标签污染。
                注意：若安装了官方 app，冷启动会被 AAR 抢走，唯有前台 ReaderMode 能压过。
            -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="https" android:host="uc-zhuli.whxinna.com" />
                <data android:scheme="http" android:host="uc-zhuli.whxinna.com" />
            </intent-filter>
            <!-- 末位兜底：NDEF 派发万一失败时，仍能由 TAG_DISCOVERED 拉起 -->
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>
